# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage
playwright-report
test-results

# Turbo
.turbo

# TypeScript incremental
*.tsbuildinfo

# pnpm local store (should not live in repo root)
.pnpm-store

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*

# Misc
.DS_Store
*.pem

# IDE / local tooling
.idea/

# Backend codegen maps — template mode: do NOT commit (CI: check:forbidden on tracked copies).
# Ship only config/domain-map.example.json + config/repository-plan.example.json.
# If either file was ever committed, run: git rm --cached -- config/domain-map.json
# Forks: remove these two ignore lines if you intentionally version real maps.
config/domain-map.json
config/repository-plan.json

# Log files (root or packages)
*.log
