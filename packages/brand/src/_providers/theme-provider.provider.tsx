/**
 * Compatibility re-export — implementation lives in `@workspace/core`.
 *
 * Prefer importing from `@workspace/core/components/theme-provider` in new code.
 *
 * @see packages/core/AGENTS.md
 */
export { ThemeProvider, useTheme } from "@workspace/core/components/theme-provider"
