"use server"

/**
 * Placeholder for the documented next-safe-action pattern.
 * Wire `authActionClient` from `@workspace/supabase-auth` when that export exists,
 * then replace this stub with a real action.
 */
export async function createExampleActionPlaceholder(): Promise<{ ok: true }> {
  return { ok: true }
}
