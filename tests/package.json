{
  "name": "@workspace/tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:integration": "mkdir -p coverage/.tmp && vitest run --config vitest.integration.config.ts",
    "test:rls": "mkdir -p coverage/.tmp && vitest run --config vitest.rls.config.ts"
  },
  "devDependencies": {
    "@supabase/ssr": "^0.10.2",
    "@types/node": "^25.6.0",
    "@workspace/supabase-auth": "workspace:*",
    "@workspace/supabase-data": "workspace:*",
    "@workspace/supabase-infra": "workspace:*",
    "@workspace/test-utils": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "@workspace/vitest-config": "workspace:*",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
